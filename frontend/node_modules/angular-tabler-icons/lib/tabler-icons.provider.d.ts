import { Provider } from '@angular/core';
import { OptionIcons, Options } from './options.interfaces';
export declare class TablerIconsProvider {
    static pick(icons: OptionIcons, options?: Options): Provider;
}
